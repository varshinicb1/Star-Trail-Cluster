#ifndef SECRETS_H
#define SECRETS_H

// Copy this file to secrets.h (gitignored) and fill in your real values.
// secrets.h is never committed — config.h #includes it.

#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// Optional: OpenWeatherMap API key for live sea-level pressure correction.
#define WEATHER_API_KEY ""

#endif // SECRETS_H
